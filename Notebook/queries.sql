CREATE TABLE haunted (
rank varchar,
city text,
country text,
description text,
location text,
state text,
longitude varchar,
latitude varchar
);

CREATE TABLE ufos (
summary text,
country text,
city text,
state text,
shape text,
date_time date,
longitude varchar,
latitude varchar
);



CREATE TABLE bigfoot (
title text,
classification text,
timestamp date,
longitude varchar,
latitude varchar
);

