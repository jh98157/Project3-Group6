CREATE TABLE haunted (
rank INT PRIMARY KEY,
city text,
country text,
description text,
location text,
state text,
longitude bigint,
latitude bigint
);

CREATE TABLE ufos (
summary text,
country text,
city text,
state text,
shape text,
date_time date,
longitude bigint,
latitude bigint
);

CREATE TABLE bigfoot (
title text,
classificatioin text,
timestamp date,
longitude bigint,
latitude bigint
);

